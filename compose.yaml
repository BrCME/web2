services:
  sql:
    image: postgres:17.5-alpine3.22
    container_name: sofia-sql
    restart: always
    environment:
      - POSTGRES_DB=db_sofia
      - POSTGRES_USER=user_sofia
      - POSTGRES_PASSWORD=P455W0RD_S0F14
    ports:
      - 5444:5432
    healthcheck:
      test: ["CMD-SHELL", "pg_isready", "-d", "db_sofia"]
      interval: 10s
      timeout: 5s
      retries: 3
    volumes:
      - localdata:/var/lib/postgres/data
    networks:
      - localnet

  cache:
    image: redis:8.0.3-alpine3.21
    # image: valkey:8-alpine3.22
    # image: memcached:1.6.39-alpine3.22
    container_name: sofia-cache
    restart: always
    mem_limit: 1024mb
    ports:
      - 6379:6379
    volumes:
      - localdata:/data
    networks:
      - localnet

# Application - Sofia
  # app:
  #   image: openjdk21
  #   container_name: sofia
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #     cache_from:
  #       - openjdk21
  #   logging:
  #     driver: json-file
  #     options:
  #       max-size: 50m
  #       max-file: 5
  #   ports:
  #     - 8080:8080
  #     - 8433:8433
    # healthcheck:
    #   test: ["curl", "pg_isready", "-d", "db_sofia"]
    #   interval: 10s
    #   timeout: 5s
    #   retries: 3
  #   depends_on:
  #     sofia-cache:
  #       condition: service_healthy
  #     sofia-sql:
  #       condition: service_healthy

networks:
  localnet:
    driver: bridge

volumes:
  localdata: